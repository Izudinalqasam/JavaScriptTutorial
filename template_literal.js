let word1 = "Suha"
let word2 = "Benyamin"

// Using template literal to use varibale inside string ``
const fullName = `${word1} ${word2}`

console.log(fullName)
document.getElementById('example').innerText = fullName;